//dac => WvOut2 wav => blackhole;
//me.dir() + "bounce/nestedLuminescence.wav" => wav.wavFilename;

/* Need constructor to execute for the
   standard normal table to be populated */
Stat stat;
/* Just build the nest and it will carry out
   the rest of the simulation */
Nest nest;

//wav.closeFile();
